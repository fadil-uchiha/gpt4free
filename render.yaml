services:
  - type: web
    name: gpt4free-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m g4f --port 8080
    envVars:
      - key: PORT
        value: 8080
    plan: free
